import {View, Text} from 'react-native';
import React from 'react';

export default function Booking_Success_Screen({route}) {
  const navigation = route.params.navigation;

  return (
    <View>
      <Text>Booking_Success_Screen</Text>
    </View>
  );
}
